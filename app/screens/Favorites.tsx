import React from "react";
import { DebounceTest } from "../components/common/Debounce";

interface FavoritesProps {}

export const Favorites = ({}: FavoritesProps) => {
  return <DebounceTest />;
};
